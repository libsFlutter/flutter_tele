# Requirements: Dialer Wrapper

**Status**: DRAFT  
**Type**: SDD (Spec-Driven Development)  
**Module**: dialer  
**Generated**: 2026-03-04 by /legacy

---

## Overview

The dialer module provides a thin wrapper around the `flutter_dialer` package for default dialer detection and management on Android. It enables the app to check and request default dialer status, which is required for full telephony integration on Android devices.

---

## Functional Requirements

### R1: Default Dialer Detection

**Description**: Check if this app is the system's default dialer.

**Requirements**:
- R1.1: `isDefaultDialer()` static method
- R1.2: Returns `Future<bool>`
- R1.3: Delegates to `FlutterDialer.isDefaultDialer()`
- R1.4: Returns `true` if app is default dialer, `false` otherwise
- R1.5: Android-specific functionality

**Source**: `lib/src/dialer.dart`

---

### R2: Set Default Dialer

**Description**: Set this app as the system's default dialer.

**Requirements**:
- R2.1: `setDefaultDialer()` static method
- R2.2: Returns `Future<bool>`
- R2.3: Delegates to `FlutterDialer.setDefaultDialer()`
- R2.4: Returns `true` on success, `false` on failure
- R2.5: May open system settings UI

**Source**: `lib/src/dialer.dart`

---

### R3: Capability Check

**Description**: Check if this app can be set as the default dialer.

**Requirements**:
- R3.1: `canSetDefaultDialer()` static method
- R3.2: Returns `Future<bool>`
- R3.3: Delegates to `FlutterDialer.canSetDefaultDialer()`
- R3.4: Returns `true` if capable, `false` otherwise
- R3.5: Checks Android version, permissions, manifest configuration

**Source**: `lib/src/dialer.dart`

---

### R4: Request Default Dialer

**Description**: Request to become the default dialer (opens system dialog).

**Requirements**:
- R4.1: `requestDefaultDialer()` static method
- R4.2: Returns `Future<bool>`
- R4.3: Currently delegates to `FlutterDialer.setDefaultDialer()` (same as R2)
- R4.4: Should open system dialog for user confirmation
- R4.5: Returns `true` if user grants, `false` if denied

**Current Implementation Note**: Currently calls same method as `setDefaultDialer()`.

**Source**: `lib/src/dialer.dart`

---

### R5: External Dependency

**Description**: Depend on `flutter_dialer` package for implementation.

**Requirements**:
- R5.1: Import `package:flutter_dialer/flutter_dialer.dart`
- R5.2: Delegate all operations to `FlutterDialer` class
- R5.3: No native code implementation in this module
- R5.4: Version compatibility with flutter_dialer ^2.0.0

**Source**: `lib/src/dialer.dart`, `pubspec.yaml`

---

### R6: Static API

**Description**: Provide static-only API (no instantiation required).

**Requirements**:
- R6.1: All methods are static
- R6.2: No instance methods
- R6.3: No state management
- R6.4: No constructor needed

**Source**: `lib/src/dialer.dart`

---

## Non-Functional Requirements

### NFR1: Platform Compatibility

- NFR1.1: Android-only functionality (iOS returns false/unsupported)
- NFR1.2: Support Android 8.0+ (Oreo/API 26+) for InCallService compatibility
- NFR1.3: Handle different Android manufacturer dialer implementations

### NFR2: Error Handling

- NFR2.1: Propagate exceptions from flutter_dialer
- NFR2.2: Return false on errors (don't throw for boolean methods)
- NFR2.3: Log errors for debugging (flutter_dialer responsibility)

### NFR3: Maintainability

- NFR3.1: Simple wrapper pattern (easy to understand)
- NFR3.2: Consistent method naming
- NFR3.3: Document Android-specific behavior

### NFR4: Documentation

- NFR4.1: Add dartdoc comments to all public methods
- NFR4.2: Document platform limitations
- NFR4.3: Provide usage examples

---

## Dependencies

- **flutter_dialer**: ^2.0.0+101 (external package)
- **Android**: Default dialer system API

---

## Open Questions

1. **Method naming**: Should `requestDefaultDialer()` have different implementation than `setDefaultDialer()`?
   - **Current**: Both call `FlutterDialer.setDefaultDialer()`
   - **Expected**: `requestDefaultDialer()` should open system dialog
   - **Recommendation**: Clarify intended behavior or rename methods

2. **Error handling**: Should wrapper add error handling or propagate exceptions?
   - **Current**: Exceptions propagate directly
   - **Recommendation**: Document exception behavior or add try-catch

3. **iOS behavior**: What should methods return on iOS?
   - **Current**: Not documented
   - **Recommendation**: Document iOS behavior (likely always false)

---

*Generated by /legacy reverse engineering*
